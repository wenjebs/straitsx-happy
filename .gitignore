node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# local state
*.db
*.db-journal
*.sqlite
*.sqlite3
closer-runs/

# a saved browser profile holds live session cookies — treat it like a password
closer-profile/
closer-profiles/

# raw issuer responses — may contain card material, never commit
card-responses/

# research scratch / third-party docs — not ours to republish
research-raw.json
StraitsX-docs.pdf
design-doc.html
plain-doc.html

.DS_Store
*.log
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
*-secrets.json
.vscode/
.idea/
